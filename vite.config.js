import { defineConfig } from 'vite'

export default defineConfig({
    // Remplace 'mon-portfolio' par le nom EXACT de ton dépôt GitHub entre les slashes
    base: 'https://github.com/TanguyBonfitt0/portfolio-tanguy.git',
})